# @dashflow/core-pro

Commercial companion to `@dashflow/core`. Built from `src/pro/`. Peer-depends on the free package.

**Do not `npm publish` to npmjs.** After X05, publish privately (GitHub Packages). See `docs/NPM_PUBLISH.md`.
